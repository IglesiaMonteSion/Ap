{
  "version": "6.14.2",
  "released": "2026-07-21",
  "notes": "6.14.2 (CHICO/SEGURIDAD — tarea #198 [S11], cadena de suministro. Tooling/CI/infra de build; sin cambio de codigo Rust logico, protocolo, consenso ni wire. (1) TOOLCHAIN PINNEADO: rust-toolchain.toml fija el compilador EXACTO (1.94.1) que rustup usa en cualquier maquina/Docker/CI + Dockerfile pasa de rust:bookworm (flotante) a rust:1.94-bookworm — junto con el Cargo.lock committeado + --locked (ya en el Dockerfile y el CI), los dos inputs que hacen a un build reproducible (compilador y deps) quedan fijos. (2) SBOM deploy/gen-sbom.sh: inventario CycloneDX 1.5 de TODA dependencia transitiva (426 componentes) con version + checksum SHA-256, derivado SOLO del Cargo.lock -> determinista (dos corridas del mismo lock = archivo byte-identico, sin timestamp), sin herramienta externa ni red (parsea con python3). Verificado: 426 componentes, oqs con purl+SHA-256, las 14 crates del workspace sin purl (correcto para locales). CI lo genera y sube como artefacto (job sbom nuevo). (3) CODEOWNERS .github/CODEOWNERS: exige review del maintainer para consenso/cripto/ejecucion/stark/nodo + Cargo.lock/toolchain/CI/Dockerfile/deploy (reemplazar @owner por el handle real + activar branch protection). (4) FIRMA GPG deploy/sign-release.sh: firma un manifiesto de release (hashes de Cargo.lock+Dockerfile+SBOM) y opcional la tag; requiere la clave GPG del maintainer (la firma es humana) + doc de git commit.gpgsign. (5) PIN DE DESCARGAS ya existia (cloudflared version fija+SHA256, docker installer pinneado). DEPLOY.md gana la seccion 'Cadena de suministro'. PENDIENTE honesto: la reproducibilidad bit-for-bit completa necesita SOURCE_DATE_EPOCH + entorno de build determinista (mas que pinnear toolchain+deps); la auditoria externa + bug bounty es #203 (proceso humano). VERIFICADO: bash -n en gen-sbom.sh/sign-release.sh; SBOM valida como CycloneDX 1.5; el build local sigue OK con el toolchain pinneado. DESPLIEGUE: nada que aplicar en la red del usuario; el pin del Dockerfile toma efecto en el proximo docker build. Sigue: #200 (fuzzing cargo-fuzz + proptest + criterion + mediciones ARM/movil)."
}
