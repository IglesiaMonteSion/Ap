{
  "version": "4.2.1",
  "released": "2026-07-15",
  "notes": "4.2.1 (chico — extiende el logo nuevo (hexágono-Q biselado 3D de v4.2.0) al resto de las superficies web que todavía usaban logos viejos; solo HTML/CSS, sin cambio de protocolo/backend). (1) **Dashboard del validador** (`qchain-node::explorer.html`, `GET /`): el hexágono-Q plano del header se reemplazó por el logo biselado nuevo, vía el mismo `<symbol id=\"qlogo\">`/`<use>` que la wallet. (2) **Wallet custodial** (`qchain-wallet::wallet.html`, `/custodial`): sus 3 instancias del logo ANTIGUO átomo/orbitales (elipses + hexágono, aún más viejo que el hexágono-Q) se reemplazaron por el biselado nuevo (welcome, loading, header). Ahora las TRES superficies web del proyecto (wallet no-custodial `/`, dashboard del nodo, wallet custodial `/custodial`) usan el mismo logo. **Ediciones puntuales exactas por instancia** (sin el regex greedy que rompió cosas en v4.2.0 — lección aplicada). **Verificado con Playwright** (nodo + wallet reales): el dashboard renderiza el logo nuevo en el header y la custodial en su welcome, **cero errores de JS** en ambas. Sin regen de WASM, clippy limpio, node+wallet compilan."
}
