{
  "version": "6.14.3",
  "released": "2026-07-21",
  "notes": "6.14.3 (CHICO/CALIDAD — tarea #200 [S12], fuzzing + property tests + benchmarks. Tooling de test/CI; CERO cambio de codigo Rust logico, protocolo, consenso ni wire. Cierra el ultimo item de la tanda 196-200. (1) PROPERTY TESTS (proptest) en qchain-core (tests/mod proptests): arbitrary_bytes_deserialize_without_panic (borsh::from_slice sobre bytes arbitrarios 0..4096 NUNCA panica el deserializador del wire) + signed_tx_roundtrips_and_txid_is_stable (48 casos generados: una tx firmada verifica, round-trip Borsh preserva TODOS los campos incluido valid_until_round, y el txid es estable ante mutacion de firma). proptest corre en CADA CI sobre inputs generados -> cobertura por-commit. (2) FUZZING CONTINUO (cargo-fuzz) en fuzz/ (crate SEPARADO del workspace, en el exclude): 2 targets libFuzzer — transaction_deserialize y message_deserialize — que deserializan bytes arbitrarios y ejercen verify_signature/txid/hash/byte_size/to_vec: ningun byte del wire debe panicar/OOM. Se corre con 'cargo +nightly fuzz run <target>' por horas/CI-cron (coverage-guided, el complemento continuo de los property tests). (3) BENCHMARKS (criterion) en crates/qchain-core/benches/signing.rs: perf reproducible del hot path con intervalos de confianza. MEDIDO en vivo: verify_signature (hybrid PQC Ed25519+ML-DSA-65) ~124us, txid (canonico SHA3) ~6us, byte_size rapido — confirma que el verify PQC domina el costo por-tx (consistente con las mediciones de TPS/RAM ya documentadas). (4) infra: proptest+criterion como dev-deps de qchain-core; [[bench]] harness=false; fuzz/ y sus artefactos al .gitignore. PENDIENTE honesto: las mediciones en HARDWARE ARM/movil real y la evaluacion NFC/secure-element (#202) dependen de hardware fisico fuera de esta sesion — se documentan como diferidas; el fuzzing por horas es un proceso de cron/nightly (proptest ya da la cobertura por-CI). VERIFICADO: los property tests pasan; el bench corre con numeros reales; clippy limpio en qchain-core all-targets; cargo build --workspace --locked OK. DESPLIEGUE: nada que aplicar en la red del usuario (tooling de test). Con esto la tanda 196-200 queda CERRADA (196 rate-limit/CSP/cache-verify, 197 endurecer wallet, 198 SBOM/firmas/toolchain, 199 limites/logrotate/attestation, 200 fuzzing/proptest/criterion). SIGUEN de la auditoria integral: #193 (separacion de claves de validador por rol + HSM, CRITICO/arquitectura grande — confirmar con el usuario), #194 (DST de rotacion + trust-anchor obligatorio), #187 (envelope de firma etiquetado por dominio, wire-breaking), #201/#202/#203 (suite_version, NFC/secure-element, auditoria externa+bug bounty)."
}
