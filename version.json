{
  "version": "6.18.2",
  "released": "2026-07-21",
  "notes": "6.18.2 (CHICO/CALIDAD — resultado de una REVISION A FONDO de toda la auditoria integral (#183-#203) con 5 agentes en paralelo verificando cada item contra el CODIGO REAL, no contra las notas. Pedido del usuario: 'revisa el listado a fondo que no quede nada pendiente nada suelto'). VEREDICTO: TODOS los items CODEABLES estan genuinamente cerrados y verificados en codigo (file:line): #186 txid canonico, #187 envelope de dominio (todos los sitios cableados + cross-verify test), #188 zeroize+harden, #189 KAT NIST, #190 decision de hash, #191 expiracion (determinista sobre `cert.vertex.round`, tickea el fee-epoch), #192 cap+borsh canonico, #193-A firmante remoto, #193-B separacion de roles (SIN riesgo de fork: author comprometido + mapa plegado en chain_id), #194 DST rotacion+trust-anchor, #195 determinismo VM, #196 rate-limit/verify-cache (solo cachea positivos)/CSP, #197 wallet hardening, #198 SBOM/toolchain, #199 limites systemd, #200 fuzz/proptest/criterion, #201 activacion por firma. BARRIDO de cabos sueltos: CERO `todo!`/`unimplemented!`; los `unreachable!`/`.expect()` de hot-path son invariantes documentados; los 5 `#[ignore]` son benchmarks; sin FIXME/HACK; workspace-members == crates/ (nada huerfano). Los diferidos (#185-B side-channel+auditoria externa, #202 NFC/hardware, #203 gates de mainnet) confirmados como genuinamente NO-codeables (hardware/procesos externos). UNICA mejora aplicada (segura, aislada): un matiz de completitud en el KAT #189 — el vector ML-DSA oficial es VALIDO (positivo) y el SLH-DSA es el INVALIDO oficial (negativo), asi que no habia par positivo+negativo por-algoritmo. Se agrega un NEGATIVO DERIVADO para ML-DSA (la firma primaria PQC): se flipea un byte de la firma del vector oficial y se exige que `verify_ml_dsa_65_component` de FALSE — probando que el verificador RECHAZA una firma manipulada, no solo que acepta una buena. crypto KAT 3/3. UNICO item codeable-ahora que sigue abierto por DECISION DE PRODUCTO (no imposibilidad): #183 Fase A (GitHub Action que audita PRs con la API de Claude — read-only, sin claves, no toca consenso); la razon 'esperar a produccion' aplica de verdad solo a la Fase B (vigilante de runtime). Se reporta al usuario para que decida; no se construye sin su OK porque toca su API key/presupuesto y su CI. SIN cambio de protocolo/consenso/wire/estado — solo un test agregado."
}
